function calculateArea(length, breadth) {
    return length * breadth;
}

var length = prompt("Enter the length:");
var breadth = prompt("Enter the breadth:");
alert("Area: " + calculateArea(length, breadth));
